<resources>
    <!-- Base application theme. -->
    <style name="user_AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/user_colorPrimary</item>
        <item name="colorPrimaryDark">@color/user_colorPrimaryDark</item>
        <item name="colorAccent">@color/user_colorAccent</item>
    </style>

    <style name="user_SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 欢迎页背景引用刚才写好的 -->
        <item name="android:windowBackground">@drawable/user_splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- <item name="android:windowIsTranslucent">true</item> --> <!-- 透明背景不要了 -->
    </style>

</resources>